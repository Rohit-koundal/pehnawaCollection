services:
  - name: pehnawaCollection
    type: web
    runtime: static
    branch: main
    buildCommand: npm ci && npm run build
    staticPublishPath: build
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
